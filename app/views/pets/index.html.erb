<h1>Take a look at all our Pets: </h1>
<div>
<% @pets.each do |pet| %>
    <a href= "https://localhost:3000/pets/<%=pet.id%>"> <%= pet.name + " " + pet.breed + " " + pet.age %> </a>
    
</div>
<% end %>
<br>
<br>
<%= button_to 'New Pet', "/cars/new", method: :get %>